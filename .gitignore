# ── Editor / IDE ──
.claude/
.vscode/
.idea/
*.code-workspace

# ── OS generated files ──
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
desktop.ini

# ── Node (if ever used locally for serving) ──
node_modules/
npm-debug.log*

# ── Local environment / secrets ──
.env
.env.local
*.local

# ── Build / cache artifacts ──
dist/
.cache/
*.log
